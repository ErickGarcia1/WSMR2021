These files made up a virus that could attack a network provided an ssh key without a password or with a weak password. Virus payload only removes images on a host
machine using crontab to run itself every minute.
